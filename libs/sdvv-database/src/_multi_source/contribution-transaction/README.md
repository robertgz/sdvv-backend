create:
* basic(temporary) transaction entity [done]
* corresponding GraphQL mutation schema [done]
* mutation resolver to log out the input and return it [done]
* transformer class on client to change input to GraphQL field names
* transformer class to change input to database field names
* database insert functions
* database function that only replaces when the input report number is greater than existing


